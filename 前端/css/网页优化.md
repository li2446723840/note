```css
body,dl,dd,ul,li,ol,h1,h2,h3,h4,h5,h6,input,p{/*去掉这些标签默认的间距*/
    margin:0;padding:0;
}
/*万能清除法*/
.clear:after{
    content:"";
    display:block;
    clear:both;
    overflow:hidden;
    visibility:hidden
    ;height:0;
}
.clear{zoom:1;}

a{text-decoration:none;}/*去掉a标签默认的下划线*/
li{list-style:none}/*去掉li标签前面默认的点*/
img{border:0;vertical-align:top;}/*去掉img标签与父元素之间的空隙*/
input{outline:none}/*去掉input标签默认的蓝色边框*/
button{outline:none}/*去掉button标签默认的蓝色边框(但是好像默认本来就没有)*/
```